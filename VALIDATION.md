# 桌面与手机版技术验证

2026-09-06，Windows 11 x64 / Python 3.14.7 / PyInstaller 6.22.2 / cryptography 50.0.1。

- 原有 36 项教学规则测试通过。
- 18 项实际后端鉴权与记录集成测试通过，新增即时轮次保存、操作日志和管理员汇总验证。
- 14 项签名更新测试通过：合法签名、密钥不匹配、内容篡改、清单篡改、更新保留数据库、自动备份、回退不回退数据、拒绝降级、模拟指针提交失败后重试、路径穿越、Windows 特殊文件名、版本合法性、签名在线清单、HTTPS 限制、引擎兼容检查（部分一项覆盖多场景）。
- 6 项手机版配置与 Service Worker 测试通过：图标/manifest、排除 API/跨域、排除 POST/未知路径、查询参数规范化、完整缓存后就绪、只清理自己的旧缓存。
- 已用打包后的 EXE 实际运行 `--self-test`：签名初始包解包、数据目录初始化、本机 HTTP 启动、教学 HTML 及 health 接口通过。
- 已实际打开桌面控制器并检查软件更新/发布页面，点击“进入学习”成功启动学习服务，浏览器显示对应内容版本和账户入口。

总计 74 项自动测试。测试账号、测试数据库、发布私钥均不装入分发包。数字签名验证与防篡改是更新包功能，不等于 Windows 商业代码签名。

没有配置公网更新源，没有发布 HTTPS 手机站点；在线清单逻辑采用签名模拟数据测试，未声称完成真实托管端的下载演练。PWA 未在 iOS/安卓真机验收，需有 HTTPS 地址后核验安装、缓存配额及触摸操作。静态手机版是独立本机档案，不与电脑 SQLite 同步；通过 Windows 班级服务器登录时使用统一 SQLite 和管理员后台。复杂内镜拖拽仍更适合鼠标端；手机优先病例、报告、识图和教程。

桌面更新期间需关闭学习窗口。未提交草稿仍只在内存；数据库备份不保存这些草稿。更新失败不会切换版本指针，但可留下一个已验证、尚未启用的版本目录，重试会复用并核验。版本回退不还原数据；已保存轮次继续保留当时的内容版本。

