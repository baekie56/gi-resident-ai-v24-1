# 中国大陆集中版部署参数

本部署把网页、账户接口和管理员后台放在同一 HTTPS 地址。SQLite 数据库必须挂载到持久化目录 `/data`，并保持单实例运行。

## 容器

- 镜像：`ghcr.io/baekie56/gi-resident-ai-v24-1:latest`
- 容器端口：`8000`
- 健康检查：`/api/health`
- 持久化挂载：`/data`
- 副本数：`1`

## 必填密钥环境变量

- `GI24_ADMIN_USERNAME`：管理员登录名，只使用学习或工作代号。
- `GI24_ADMIN_PASSWORD`：至少 10 位的管理员密码。
- `GI24_REGISTRATION_CODE`：发给本班学员的注册码。
- `GI24_SECURE_COOKIES=1`：HTTPS 公网部署必须启用。

管理员首次启动时自动创建；以后必须继续使用同一用户名和密码。环境变量应放在云平台的 Secret 配置中，不得提交到 GitHub。

## 数据与备份

数据库位于 `/data/gi24.sqlite3`，保存账户、学习档案、训练轮次、操作事件、管理员反馈和管理员发布的病例版本；上传的病例图片保存在 `/data/content-assets`。更新镜像时不得删除 `/data` 持久化卷。正式研究前应设置每日加密备份，并只使用研究编号，禁止录入患者姓名、病历号或身份证号。

管理员登录后进入“管理员内容管理”，可先保存草稿，再发布给全部学员。修改已发布病例时，学员继续使用上一发布版，直至管理员再次点击发布。停用内容只阻止开始新的训练，不删除既有训练记录。每次草稿保存、发布和停用均保留版本审计记录。

中国大陆云服务器绑定自定义域名通常需要完成 ICP 备案。平台临时 HTTPS 域名可以用于小规模技术试用；正式招募研究对象前，应完成本单位信息部门、伦理和数据管理审批。
